/**
 * Fallback when `API_BASE_URL` is not set (local tooling and tests).
 * Replace with your own user-directory API origin in production.
 */
export const DEFAULT_UPSTREAM_API_ORIGIN = "https://dummyjson.com";
